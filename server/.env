# Copy to `.env` in this folder (`server/.env`)

PORT=4000
MONGODB_URI=mongodb+srv://gbataa366:112456_Bat@challenge.bswfzkd.mongodb.net/smart_mode
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_PASSWORD=change

# Where uploaded images are stored (created automatically)
UPLOAD_DIR=./uploads

# Public URL prefix for uploads (browser loads from API origin + this path)
# Default: http://localhost:4000/uploads/...
